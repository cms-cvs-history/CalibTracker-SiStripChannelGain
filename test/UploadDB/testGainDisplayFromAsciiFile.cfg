process Test = {
    
    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet insert_logfile = { untracked string threshold = "INFO" }
    }

    source = EmptyIOVSource {
	string timetype = "runnumber"
        untracked uint32 firstRun = 1
	untracked uint32 lastRun = 20000
        uint32 interval = 1
    }
    untracked PSet maxEvents = {untracked int32 input = 100000}
    
    es_source = PoolDBESSource { 
	VPSet toGet = {{ string record = "SiStripApvGainRcd" string tag = "SiStripApvGain_TickMarksfromASCII_TBuffer_v1" }}
	
	string connect = "oracle://cms_orcoff_int2r/CMS_COND_STRIP" 
	#string connect = "sqlite_file:GainFromAsciiFile.db"    
	
	string timetype = "runnumber" 
	untracked string BlobStreamerName="TBufferBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
	}
    }
    
    module prod =  SiStripApvGainDisplay {
	untracked bool   printDebug = true
    }

       path p = { prod }

       module print = AsciiOutputModule {}
       endpath ep = { print }
}
