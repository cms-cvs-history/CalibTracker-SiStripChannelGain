process Test = {
    
#    source = EmptySource {
#	untracked int32 maxEvents = 1
#	uint64 firstValue = 1   
#	untracked uint32 numberEventsInRun = 1
#    }

    source = EmptyIOVSource {
        string timetype = "runnumber"
        uint64 firstValue = 1
	uint64 lastValue = 3
        uint64 interval = 1
    }
    
    service = MessageLogger {
	untracked vstring destinations = { "logfileread" }
	untracked PSet insert_logfile = { untracked string threshold = "INFO" }
    }
    
    service = Timing {}
    
    
    es_source = PoolDBESSource { 
   VPSet toGet = {{ string record = "SiStripApvGainRcd" string tag = "mytest_g" }}
	
	string connect = "sqlite_file:insert_dbfile"    
	#
	string timetype = "runnumber" 

	 untracked string BlobStreamerName="TBufferBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
	   	}
    }
    
             module prod =  SiStripApvGainReader {

                  untracked bool   printDebug = false

                 }

       path p = { prod }

       module print = AsciiOutputModule {}
       endpath ep = { print }
}
